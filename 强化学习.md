# 强化学习

RLHF: https://proceedings.neurips.cc/paper_files/paper/2022/file/b1efde53be364a73914f58805a001731-Paper-Conference.pdf

PPO: https://arxiv.org/abs/1707.06347

DPO: https://arxiv.org/abs/2305.18290

GRPO: https://arxiv.org/abs/2402.03300

**SFT（监督微调）和 RLHF（强化学习人类反馈）在目标和数据形式上有什么本质区别？为什么有了 SFT 还需要 RL？**
SFT 的目标是**模仿**，通过最小化 Next Token Prediction 的交叉熵损失，让模型学会“像人类一样说话”，其数据形式是 `<Prompt, Answer>` 的单条指令对；而 RLHF 的目标是**优化**，通过最大化奖励信号，让模型学会“符合人类偏好”，其数据形式通常是 `<Prompt, Chosen, Rejected>` 的偏好对或奖励分数。
之所以有了 SFT 还需要 RL，是因为 SFT 只能让模型达到人类标注数据的平均水平，无法突破上限，且难以处理多目标权衡（如既要 helpful 又要 harmless）。RL 引入了探索机制和全局奖励信号，能让模型在 SFT 的基础上进一步对齐人类价值观。

**为什么大模型 RL 训练中必须引入 KL 散度惩罚（KL Penalty）？如果不加会发生什么？**
引入 KL 散度惩罚是为了**约束模型不要偏离原始 SFT 模型太远**。
如果不加 KL 惩罚，模型为了最大化奖励分数，极易发生 **Reward Hacking（奖励黑客）** 现象：模型可能会发现某些特定的短语、重复的词汇或无意义的字符组合能骗过 Reward Model 拿到高分，从而疯狂生成这些内容，导致输出多样性丧失，甚至完全丧失语言能力，变成只会刷分的“复读机”。KL 惩罚项强制模型在提升奖励的同时，保持生成分布的自然性和多样性。

**什么是“稀疏奖励”（Sparse Reward）问题？在代码生成或数学推理任务中，如何解决这个问题？**
稀疏奖励是指模型只有在完成整个任务（如写完代码并运行通过、算出最终答案）后才能得到反馈，中间生成的每一步 Token 都没有即时奖励。解决方法通常有：引入Critic模型，过程奖励，组内相对优化（GRPO）

**为什么 RLHF 阶段通常选择 PPO 算法，而不是 DQN 或 A3C？**

选择 PPO 主要基于**动作空间的特性**和**训练的稳定性**两点考虑。
首先，大语言模型的动作空间是词表大小（几万到几十万），而且是离散的。DQN 需要遍历所有动作来计算 Q 值，这在计算上是不可行的；其次，PPO 引入了 **Clipping（截断）机制**。大模型参数量巨大，策略更新稍微大一点就容易导致‘灾难性遗忘’或者模式坍塌。PPO 通过限制新旧策略的比值变化范围，保证了在大规模参数下的训练稳定性。

**请通俗解释一下 PPO 中的 Clip 操作是做什么的？如果不加会怎样？**

Clip 操作的核心目的是**防止策略更新幅度过大**。PPO 计算新旧策略的概率比值，如果某个动作奖励很高，普通算法可能会让比值瞬间变得很大，导致模型参数剧烈震荡，甚至为了刷分而只生成重复的特定词汇（Reward Hacking）。PPO 强制将比值限定在$[1\epsilon, 1+\epsilon] \epsilon通常为0.2$，防止模型多样性急剧下降、语言崩溃，或者过度拟合 Reward Model 的漏洞。

**Critic 模型和 Reward Model (RM) 有什么区别？为什么有了 RM 还需要 Critic？**

**Reward Model (RM)** 是‘裁判’，它在整句话生成完后，给出一个**最终的绝对分数**，它的参数在 PPO 训练阶段是冻结的；**Critic 模型** 是‘评论家’，它的作用是预测**当前状态的价值期望**它是动态训练的，目标是拟合 RM 的打分。如果没有 Critic 做基线（Baseline），所有 Token 只能平分奖励，训练信号的方差会极大，导致模型收敛非常慢甚至不收敛。

使用Critic是为了解决**信用分配（Credit Assignment）**问题和**降低方差**。如果没有 Critic 做基线（Baseline），所有 Token 只能平分奖励，训练信号的方差会极大，导致模型收敛非常慢甚至不收敛。

**PPO 训练非常吃显存，主要消耗在哪里？有哪些优化手段？**

PPO 显存占用大的主要原因是需要同时加载 **4 个模型**：Actor、Critic、Reference 和 Reward Model，而且还要存它们的梯度、优化器状态和激活值。常见的优化手段有：模型量化（Reference和Reward模型是冻结的），DeepSpeed Zero优化（将优化器状态、梯度甚至模型参数分片存储到多张卡上）， 梯度检查点（不保存中间激活值，用时间换空间）

 **什么是 Reward Hacking？你在实验中怎么发现并解决的？**

Reward Hacking 是指模型找到了 Reward Model 的漏洞，生成了**人类觉得质量很差但 RM 给分很高**的内容。常见现象包括：无限重复某个高分词汇、生成看似高深实则无意义的车轱辘话、或者忽略指令只堆砌关键词。

解决方案通常有：加强正则化（限制步子迈太大）、改进数据、混合预训练数据(PPO-ptx)

**请简述DPO和RLHF (PPO) 的核心区别是什么？**

核心区别在于**优化路径**和**训练范式**。

+ 传统的RLHF (PPO) 是**间接优化**：它需要先训练独立的奖励模型，然后在强化学习阶段，通过PPO算法**在线采样**，利用奖励模型的打分来更新策略模型。

+ 而DPO是**直接优化**：它通过数学推导将偏好对齐问题转化为了一个**监督学习问题**，**省去了显式的奖励模型和强化学习采样过程**，直接在**离线偏好数据集**上优化策略模型。

  >训练流程+算法类型+显存/资源+稳定性+数学本质

**DPO真的完全不需要奖励模型了吗？**

从**工程实现**上看，确实不需要单独训练、存储和维护一个奖励模型，这大大降低了成本。但从**理论本质**上看，DPO并没有抛弃奖励机制。它假设**语言模型本身隐含了一个奖励函数**。DPO的损失函数实际上是在优化这个‘**隐式奖励**’，使其符合人类偏好（**奖励最大化转化为概率比值最大化**）。

**DPO 的缺点或局限性有哪些？**

DPO虽然高效，但也有明显的局限性，主要有三点：
第一，**泛化能力较弱（OOD问题）**。由于没有独立的奖励模型进行全局约束，DPO**在训练分布之外（Out-of-Distribution）的数据上表现不如PPO**，容易对未见过的数据产生错误的过高奖励估计。
第二，**对数据偏差极度敏感**。如果偏好数据中存在长度偏差（比如长回答容易被选为优选），DPO会直接过拟合这种统计捷径，导致模型生成冗长内容，而PPO中的奖励模型有一定的去噪和抽象能力。
第三，**依赖高质量的SFT初始化**。DPO本质上是**在SFT模型附近做局部优化**，缺乏PPO那样的在线探索（Exploration）能力。如果SFT底座太差，DPO很难‘力挽狂澜’。

**为什么DPO训练比PPO稳定？**

主要是因为**优化目标的性质不同**。PPO涉及**复杂的强化学习组件**：重要性采样、Clip截断、价值函数拟合等，这些环节误差容易累积，且**超参数（如KL系数、Learning Rate）之间高度耦合**，稍有不慎就会导致训练崩溃或模式坍塌；
而DPO将问题转化为了标准的**二元交叉熵损失（Binary Cross-Entropy）**。这是深度学习中最成熟、最稳定的监督学习目标，没有方差估计问题，也不需要在线采样带来的分布偏移，因此收敛非常平稳，几乎不会崩溃。

**如果只有单条反馈数据（没有成对的 Prefer/Reject），能用DPO吗？**

**标准的DPO算法不能直接使用**，因为它的损失函数严格定义为偏好对数据的概率比值优化，缺少负样本就无法计算梯度。但在实际工程中有两种解决方案：一是构造伪负样本，让模型自己对同一个prompt生成多个回答，选取最好的作为prefer，其他作为reject；二是使用改进算法如KTO，它就是专门为了解决非成对偏好数据而设计的。

**DPO中的 $\beta$ 参数过大或过小会发生什么？**

$\beta$ 控制着**模型偏离参考模型(SFT)的程度**，类似于KL散度的惩罚系数。如果$\beta$过小则约束太弱，，模型会过度拟合偏好数据，可能导致‘奖励黑客’（Reward Hacking），比如为了得高分而胡言乱语，或者输出多样性急剧下降（模式坍塌）；如果$\beta$过大则约束太强，模型不敢更新，几乎退化为原始的SFT模型，学不到新的偏好对齐能力，Loss降不下去。
通常需要根据数据集大小和质量，在0.1到0.5之间进行网格搜索寻找最佳值。

**为什么DPO要加KL散度限制？**

原因有三点：

+ **防止过拟合**：偏好数据（Preference Data）通常**很少且含有噪声**。如果没有 KL 限制，模型会强行记忆训练数据中的特定句式，导致在测试集上泛化能力下降。
+ **保持语言能力**：KL 限制确保优化后的模型依然“像人话”，不会因为过度追求某个特定的偏好信号而变成只会输出关键词的“机器”
+ 数学推导的必然结果：DPO 是从带 KL 约束的奖励最大化问题推导出来的。

**在实际工程中，DPO的Reference Model必须和Policy Model初始化一样吗？**

是的，必须完全一致。DPO的理论推导基于一个前提：**策略模型是从参考模型出发进行更新的**。Reference Model 代表了‘初始分布’，**用于计算KL散度约束**。
如果初始化不一致，那么计算出的概率比值就失去了基准，导致优化目标偏差，模型可能会学到错误的方向，甚至无法收敛。所以在代码实现中，通常在训练开始前 deep copy 一份 Policy Model 的权重作为冻结的 Reference Model。

**什么场景更适合使用DPO？什么时候更适合用PPO？请举例说明。**

选择DPO还是PPO，主要取决于**数据形态、资源预算**以及**对泛化能力的要求**。它们不是简单的替代关系，而是适用场景不同。

适合用DPO的场景：拥有高质量的**离线成对偏好数据**，追求**工程落地效率**，算力资源有限，且测试分布与训练分布较为一致。例如：垂直领域客服微调，风格迁移与语气对齐。

适合用PPO的场景：缺乏高质量成对数据，需要**在线探索**，处理**开放域/分布外（OOD）任务**，或者奖励信号来自**复杂规则/环境反馈**而非单纯的人类标注。例如：代码生成与调试，开放域创意写作与安全对齐，数据稀缺或只有单点反馈。

**请简述 GRPO 的核心思想，它和 PPO 最大的区别是什么？**

GRPO (Group Relative Policy Optimization) 是 DeepSeek 提出的一种高效强化学习算法。它的核心创新在于**去除了 PPO 中必需的 Critic (Value) 模型**，转而通过**对同一个问题采样一组输出（Group）**，利用这组输出的奖励分布统计量（均值和标准差）来计算相对优势（Advantage）。

与PPO的最大区别：在**架构上**PPO 需要训练一个与 Actor 同等大小的 Critic 模型，而GRPO **完全不需要 Critic 模型**；**优势计算**上，PPO依赖价值网络的估计，容易引入误差，GRPO直接基于**组内样本的相对排名**计算，无需拟合价值函数；**资源使用**上，GRPO少加载一个大模型及其优化器状态，节省了50%的显存。

**为什么 GRPO 要去掉 Critic 模型？去掉后会不会导致训练不稳定？**

在 LLM 训练中，**Critic 模型占用的显存和 Actor 一样大**，且需要额外的优化器状态，这对显存是巨大浪费；LLM 生成任务通常是**稀疏奖励**（只有最后一步有分），中间步骤的价值极难准确估计。**Critic 模型往往收敛慢或偏差大**，反而引入了噪声，拖累 PPO 的性能。

去掉 Critic **不仅没有降低稳定性，反而提升了稳定性**。GRPO 利用**组内差分**消除了基线偏差。只要 Group Size ( G*G* ) 足够大（如 64），组内的均值和标准差就能提供一个非常稳健的相对基准。

 **GRPO 和 DPO 有什么区别？既然 DPO 也不需要 Critic，为什么还要用 GRPO？**

**DPO (Offline)**基于**静态偏好数据集**进行偏好优化。通过最大化似然比来隐式优化 Reward，**不能探索**数据分布之外的新解；**GRPO (Online)**基于**在线采样**。模型在训练过程中不断生成新数据，并根据实时反馈（如代码运行结果、答案比对）更新策略。

GRPO在**探索能力**、**奖励信号粒度**、**性能上限**上优于DPO。对于数学、代码等**可验证任务 (Verifiable Tasks)**，正确答案可能不在初始数据集中。GRPO 可以通过不断试错（Sampling）找到新的正确解；GRPO 可以直接利用**规则奖励**（如答案对错、代码通过率），信号绝对准确；DPO 依赖人类偏好数据，可能存在标注噪声或一致性差的问题；DeepSeek的实验表明在推理任务上，Online RL (GRPO) 在解决高难度问题时性能显著优于DPO。

**GRPO 中的 Group Size (G) 对训练有什么影响？如何设置？**

- **统计稳定性**： G 越小，组内均值和方差的估计噪声越大，导致 Advantage 波动剧烈，训练难以收敛。
- **计算成本**： G越大，单次迭代需要生成的 Token 数量越多，**推理时间线性增加**，训练速度变慢。
- **显存占用**： G增大会略微增加激活值（Activation）的显存占用，但远小于加载 Critic 模型的开销。

通常设置在 **16 ~ 64** 之间。如果显存允许，优先增大 G 以保证稳定性；如果训练速度太慢，可适当减小 G ，但需配合调整学习率或增加 Clip range 来缓解震荡。一般不建议低于 8。

**GRPO 的损失函数中，KL 散度项是如何处理的？为什么要单独减去而不是加在 Reward 里？**

GRPO 将 KL 散度惩罚项 **直接加在 Loss 函数的末尾**，而不是像传统 PPO 那样将其从 Reward 中扣除 。首先使得优势是纯净的，PPO的设置导致Critic或统计量难以区分“任务做得好”还是“只是离参考模型近”；另外这种解耦优化让优势**纯粹反映任务奖励的相对优劣**，物理意义更清晰，训练更稳定。

